
task clearJar(type: Delete) {
    delete "build/libs/${project.name}.jar"
}

task makeJar(type: Copy, dependsOn: [clearJar, build]) {
    from('build/bundles/release/')
    into('build/libs/')
    include('classes.jar')
    rename ('classes.jar', "${project.name}.jar")
}